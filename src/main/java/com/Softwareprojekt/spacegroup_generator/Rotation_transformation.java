/*
package com.Softwareprojekt.spacegroup_generator;


import org.la4j.vector.dense.BasicVector;

import java.util.LinkedList;
import java.util.Vector;

public class Rotation_transformation {
    BasicVector pos;
    String axis;
    Double rotaion;

    Rotation_transformation(double [] cords, String axis){
       pos = new BasicVector(cords);
       switch (axis){
        case "x":
                  break;
        case "y":
            break;
        default:

    }

    public LinkedList<BasicVector> rotate_point (BasicVector point) {
        BasicVector delta = (BasicVector) pos.subtract(point);
        LinkedList <BasicVector> new_points;
    }
}
}
*/
